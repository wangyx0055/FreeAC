<!DOCTYPE html>
<html >
  <head>
		<meta charset="utf-8">
		<title>FX-Converter</title>
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>

  </head>

	<body>

      <div class="wrapper">
          <div class="tv">
      <p class="centered" >Currency Converter</p>

      <!-- Date button -->
      <div class="row divider dateB">
      <div class="dropdown" >
        <button class="btn btn-info dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" ><%= @d_array[0] %><span class="caret"></span>
        </button>

      <ul class="dropdown-menu scrollable-menu" aria-labelledby="dropdownMenu1" >
        <% @d_array.each do |date_s| %>
        <li><a onclick="updateDateButton('<%= date_s.to_s %>')"><%= date_s %></a></li>
        <% end %>

      </ul>
      </div>

      </div>

      <!--Input - Cur -->
      <div class="row divider">
        <div class="col-sm-6">
        <input id="input_money" type="text"class="form-control" placeholder="Amount to be coverted">
      </div>
      <p class="col-sm-1 text_b">From:</p>

      <div class=" col-sm-2 text-center zero_pad dropdown " >
      <button id="fromButton" type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" ><%= @c_array[0] %><span class="caret"></span>
      </button>
      <ul class="dropdown-menu scrollable-menu"  aria-labelledby="fromButton" >
          <% @c_array.each do |currency| %>
        <li><a onclick="updateFromButton('<%= currency %>')"><%= currency %></a></li>
          <% end %>
      </ul>
      </div>

      <p class="col-sm-1 text_b"> to </p>

      <div class=" col-sm-2  zero_pad dropdown " >
      <button id="toButton" type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" ><%= @c_array[6] %><span class="caret"></span>
      </button>
      <ul class="dropdown-menu scrollable-menu"  aria-labelledby="toButton" >
          <% @c_array.each do |currency| %>
        <li><a onclick="updateToButton('<%= currency %>')"><%= currency %></a></li>
          <% end %>
      </ul>
      </div>

      </div>

    <!--Submit Button-->
    <div class="row divider centered">
    <label class="col-sm-offset-5 col-sm-2 btn btn-danger " onclick=convert()> Convert!!</label>
    </div>

    <!--Results-->
    <div id="results" class="centered_bot" > </div>

    </div>
    </div>

  </body>
</html>
